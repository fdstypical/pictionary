@require '~@/assets/stylus/mixins/mixins';

.base-layout {
  width: 100%;
  min-height: 100vh;
  padding-left: 20px;
  padding-right: 20px;

  &__main {
    padding-top: 25px;
    padding-bottom: 25px;
    flexy(center, center);
  }

  &__content {
    width: 100%;
  }
}